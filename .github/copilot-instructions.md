# Bun + TypeScript Project

This is a Bun project with TypeScript support.

## Project Setup
- Runtime: Bun
- Language: TypeScript
- Package Manager: Bun

## Development
Run `bun run dev` to start the development server.
Run `bun run build` to build the project.
